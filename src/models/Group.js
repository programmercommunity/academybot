// the bot should be in all sub-groups, to track the users further

/*
group: {
    name
    createdAt
    purpose
    is_general
    platform: "telegram"|"discord"|"whatever"
    owner: userModel
    ...
}
*/

import mongoose from "mongoose";
const groupSchema = new mongoose.Schema({

})
